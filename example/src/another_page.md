# Another Page

This chapter does **not** contain a `{{util-banner}}` block, yet the banner
from `introduction.md` appears here as well — because `util-banner` injects
banners globally.

Dismissing the banner on this page also hides it on the Introduction page,
and vice versa.
